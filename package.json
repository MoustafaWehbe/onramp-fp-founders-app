{
  "name": "starter-kit",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "worker": "npm run worker --workspace=@starter-kit/api",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "ci": "npm run lint && npm run test && npm run build",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "packageManager": "npm@10.8.0"
}
