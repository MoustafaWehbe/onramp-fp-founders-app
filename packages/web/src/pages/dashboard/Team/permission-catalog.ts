/**
 * Moved into ../../../lib/permissions, where the route guards, the sidebar and
 * the "no access" screens read the same labels. Re-exported here so the role
 * editor's existing import keeps working.
 */
export { PERMISSION_CATALOG } from "../../../lib/permissions";
